import { RefAttributes } from 'react';

type ChaiseTreeviewProps = {
  // TODO the props of this component and their types and documentation
} & RefAttributes<HTMLUListElement>;

const ChaiseTreeview = ({
  // TODO the props
}: ChaiseTreeviewProps): JSX.Element => {

  return (<div>this is a tree!</div>);
};

export default ChaiseTreeview;
